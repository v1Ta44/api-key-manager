# 前端深度重构续接

基础提交：`8c096ed`。用户允许完整替换旧界面，并明确选择 **B「温润档案风」，继续后续重构**。不得重复询问视觉方向或重构授权。

## 当前结果

- 最新授权：用户要求「跳过所有人工检查，进行发布」，并指定 GitHub `v1Ta44/api-key-manager`。剩余人工检查记录为用户明确豁免、仍未验证；不再作为阻止该次发布的门槛。正式 v2.0.0 包已准备到 `dist/v2.0.0/`，程序字节与已验证候选一致，GitHub 远端发布完成后另留回执。
- 最新发布候选：`artifacts/release-check-20260922-223757-8966/`。155 项单元测试、两种候选各 79 项窗口检查、58 条发行打包检查通过；SDK 固定 9.0.318，运行时与生产包依赖更新为 9.0.20。完整状态见 [release-readiness.md](docs/release-readiness.md)，发行说明见 [release-notes-2.0.md](docs/release-notes-2.0.md)。当前是自动化通过、环境验收待完成，未发布。
- 上轮顶栏候选：原生 Windows 顶栏已替换为 B 风格的一体式标题栏，深浅主题共用已有配色。候选为 `artifacts/publish/20260922-221251-1989/`，当时 150 项测试及两种打包各 70 项窗口检查通过。基本拖动/双击/最小化/关闭如今已由用户确认；贴边与多屏缩放仍未实测。
- 阶段 0/1 完成；阶段 2–4 的生产实现完成；阶段 5 的自动化和渲染已验证，用户已确认核心五步及两项自动保护；缩放/多屏/辅助技术等环境项目未完成；阶段 6 的默认入口切换、旧界面移除、文档/脚本迁移和本地候选打包已完成。
- 不把整个计划标成已验收。当前是可运行的本地替换候选，未接触真实用户库，没有提交、推送、安装或外部发布。
- 单一计划：[frontend-refactor-plan.md](docs/frontend-refactor-plan.md)。完整结果与缺口：[frontend-refactor-results.md](docs/frontend-refactor-results.md)。格式与失败合同：[refactor-baseline.md](docs/refactor-baseline.md)。

## 已实现

`Application/VaultSession` 拥有会话密码、候选事务、导入预览、备份和改密；`FileVaultStorage` 候选回读后替换；`VaultLease` 同路径排他。Core 的格式/算法不变。

`WorkspaceViewModel` 管页面状态与用例，`WorkspaceWindow` 管控件/焦点/尺寸与平台适配。新页面覆盖原有能力，增加搜索与平台/标签/到期筛选、单条显示 10 秒、按钮旁复制反馈。主题启动/导入恢复、保存失败重试、锁定清除草稿、剪贴板匹配清理与有限重试已接通。

## 验证

- 150 项自动测试通过；Debug/Release 构建 0 警告、0 错误。
- 生产窗口 54 项检查通过，无绑定警告；900/1180/1600 宽、600/760 高、11 张截图，见 `artifacts/workspace-review/`。
- 29 项历史原型检查通过。
- 本机 Release 1000 条搜索到布局 P95：框架依赖 82.7 ms、自包含 54.0 ms；实际仅实例化 7 个可见行。数据与机器条件详见实施记录。
- 最终本地产物：`artifacts/publish/20260922-211218-0217/fd/` 与 `sc/`；都通过基础自检和生产窗口检查。
- 额外以新合成目录实际启动框架依赖候选版 `--demo --dir` 并正常关闭，见 `artifacts/startup-c5f74502401d4574965ebcec22990e3e/startup-result.json`。
- 核心键鼠路径、密钥复制、锁定后保留随后复制内容、关闭重开持久保存：用户明确确认五步通过。剪贴板到时清理、真实闲置锁定及未保存草稿清除也已由用户确认通过。DPI/多屏/读屏、完整 Tab 顺序及剪贴板争用仍待验证。自动锁定自动检查使用注入时间，揭示隐藏使用实际 DispatcherTimer。

## 试用与后续

运行 `./tools/preview-frontend.ps1` 打开正式新版的隔离演示，密码 `demo`；`-Prototype` 可回看阶段 1 对照。`verify-workspace.ps1` 重跑生产窗口检查；`verify-publish.ps1` 创建新的本地打包目录，不终止用户进程或删除旧产物。

核心五步及两项自动保护已经由用户明确接受；本轮核心功能与自动保护验收已结束。余下是系统缩放、多屏和辅助技术等环境检查。B 方向已被接受；没有把范围有限的核心路径确认扩大为全量 `driver-accepted`。如继续修正，沿现有计划和结果记录，不创建新的持久子蓝图。

`runtimePersistentBlueprintBudget = 0`。无活动临时问题支线。

## 2026-09-22 下一步：实机试用已就绪

用户要求“下一步”。延续已授权的阶段 5 验收，本轮只准备可重复的核心路径试用，不扩展功能。

- 使用已经通过验证的候选 `artifacts/publish/20260922-211218-0217/fd/ApiKeyManager.exe`；没有重建或修改生产源码。
- 已启动独立试用目录 `artifacts/acceptance-20260922-213750/data/`，仅合成数据，密码 `demo`。启动时进程 22284，窗口标题正常、消息循环就绪、合成库已生成；进程当前是否仍运行以后续观察为准。
- 留下 `artifacts/acceptance-20260922-213750/reopen.cmd`，用户关闭后可继续同一库，验证修改和主题持久保存。该入口不再重复 `--demo` 初始化。
- 五步试用说明：`artifacts/acceptance-20260922-213750/试用步骤.md`。版本 SHA-256 和启动证据：同目录 `session.json`。
- 唯一退出条件：用户确认新增查找→编辑主题→复制锁定→解锁→关闭重开这条路径可用，或提供具体失败以便修正。此退出条件已由用户「这五步已通过」明确满足，证据来源为用户确认，不是代理模拟操作。
- 当时的下一检查为自动保护；如今两项保护已由用户确认。系统缩放/多屏/读屏仍未验证，原生桌面控制未开放的限制仍在。
- 本轮沿用 code-quality-workflow / ST-A0 / 库快照 2026-08-18；目标是可操作且可重开的试用对象，红线是真实库/系统偏好/用户剪贴板不由代理改写，验收是隔离启动证据和人工结果分开记录。授权来自完整重构及本轮“下一步”。

## 用户确认：核心五步已通过

- 原话：「这五步已通过」。按该明确反馈记录 `five-step-core-workflow = accepted-by-user`；逐项范围见 `artifacts/acceptance-20260922-213750/acceptance-events.jsonl`。
- 本轮只更新验收文档与会话记录，未改源码、未重建、未修改试用库的偏好或记录。原 150 项测试及 54 项窗口检查仍对应当前候选。
- 下一小步已准备：同目录 `自动保护检查.md`。用户在演示窗口将清理/锁定临时设为 5 秒/1 分钟，验证到时清理和编辑中闲置锁定；检查后可恢复 30 秒/5 分钟。
- 这两项已由用户在后续消息「两项都通过」确认。DPI、多屏、读屏、完整键盘导航与其余未测边界继续保留，不把有限范围的验收视为发版授权或所有平台通过。

## 用户确认：两项自动保护已通过

- 用户原话：「两项都通过」。确认范围为真实剪贴板到时清理、真实闲置自动锁定、锁定后未保存草稿清除且已保存记录保留。证据来源为用户实测回报。
- 同目录 `acceptance-events.jsonl` 追加原话与范围，`session.json` 更新为 `core-and-protection-accepted-by-user`；全量环境验收继续待完成。
- 当前核心功能和自动保护验收已收口。没有新故障，不重复开启新的修复任务或要求重测已通过项目；剩余缩放/多屏/读屏、完整 Tab 顺序及原报告的特殊故障边界保留为未验证。
- 本轮只更新验收记录，不改生产源码、真实库或演示设置，不重新构建，也不提交、推送或发布。短时间试用参数如需恢复，可由用户在演示中改回清理 30 秒、锁定 5 分钟。

## 2026-09-22 顶栏美化

用户要求把 Windows 原生顶栏替换为项目风格。已将窗口控制合入 64 DIP 顶栏，使用细线图标、柔和悬停色、键盘焦点和辅助名称；保留 Windows 原生拖动、边缘缩放、双击最大化及系统菜单入口。窄窗口隐藏「本地空间」标签，保留独立拖动区。

首次运行检查发现最大化铺到任务栏下方，已按当前屏幕工作区修复；本机检查从内容范围 `-7,-7 → 1927,1087` 修正为 `0,0 → 1920,1032`，与可用区域一致。未据此宣称不同缩放的多屏环境通过。

新检查：`artifacts/titlebar-review/checks.json`（70 项，11 张截图），两种打包结果：`artifacts/publish/20260922-221251-1989/publish-results.json`。新版本试用入口：`artifacts/titlebar-demo/reopen.cmd`，合成库密码 `demo`。旧验收记录及其对应程序保留。

本次只改窗口、样式、窗口检查及续接文档。使用既有 code-quality-workflow / ST-A0 收据，窗口实现属 Local Fix Only；退出条件为去除系统顶栏、深浅/窄窗口可用、原生窗口操作自动检查和原有用例通过。实现与自动检查已完成，人工标题栏验收未设置；Win11 悬停最大化按钮的 Snap 布局面板未实现，实际拖动贴边、Win+方向键、混合 DPI、多屏与读屏仍未实测。

## 2026-09-22 发布前检查

用户要求完成发布前检查，并对上一版顶栏操作明确反馈「这些操作均正常」。已将拖动、双击最大化/还原、最小化和关闭的确认追加到 `artifacts/titlebar-demo/acceptance-events.jsonl`，关联原候选哈希；此前核心及保护验收记录继续保留。

本轮完成：真实磁盘占用/失败/恢复测试，真实密码控件的辅助接口保护检查；修正 EXE 图标路径和 Windows 8.1 支持声明；固定 SDK/最低补丁/依赖锁文件；新发布脚本验证两个 ZIP、解压启动、多实例、防覆盖与重开，并附许可、说明、SHA-256 和源码文件清单。官方 SDK ZIP 只解压到 `artifacts/toolchain/dotnet-9.0.318/`，没有替换系统 .NET 安装。

统一重跑入口：`./tools/verify-release.ps1 -DotnetPath ./artifacts/toolchain/dotnet-9.0.318/dotnet.exe`。新发行候选是 2.0.0 / win-x64 / .NET 9.0.20；优先交接 sc 自包含包。自动化报告状态为 `automated-checks-passed-manual-gates-pending`。

本机验收入口为新证据目录中的 `acceptance.cmd`，密码 `demo`。额外验证 sc 包在共享运行时路径无效时仍通过 79 项窗口检查，见 `sc-without-shared-runtime/checks.json`。不据此代替另一台干净机器的验收。

剩余：系统缩放切换、多屏混合缩放、完整键盘/读屏、另一台目标系统或干净机器、签名与分发安排；已有步骤见发布检查报告末尾。xUnit v2 的 5 个测试专用依赖被标记 Legacy，报告明确保留精确版本例外；它们不进入发行包，没有由此扩展到测试框架大版本迁移。没有 Git 提交/推送、真实库迁移或外部发布。
