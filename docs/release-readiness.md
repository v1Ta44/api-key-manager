# 2.0.0 发布前检查

**发布已完成**：[GitHub v2.0.0](https://github.com/v1Ta44/api-key-manager/releases/tag/v2.0.0)，2026-09-22 22:55:58（UTC+8）；非草稿、非预发布。标签对应源码提交 `37e0c2dfaa929183578f689c275e71633789912e`。两个 ZIP、SHA256SUMS.txt、release-manifest.json、RELEASE-NOTES.md 共 5 个远端附件的状态、大小和 SHA-256 均已回读核对。本地回执为 `dist/v2.0.0/github-publication-receipt.json`。剩余人工项目为用户主动豁免，状态保持未验证。

检查日期：2026-09-22。下文保留候选验证过程；后续用户明确要求「跳过所有人工检查，进行发布」，并指定 `https://github.com/v1Ta44/api-key-manager.git`。据此将剩余人工门设为 `waived-by-user-not-tested`，允许发布 v2.0.0；不是补记测试通过。Git 提交、推送和该仓库的 Release 发布已获本轮授权，真实库迁移仍不在范围内。

正式发行包位于 `dist/v2.0.0/`，程序二进制与通过检查的候选完全一致；仅更新随包说明和 stable 发行标记。sc ZIP SHA-256：`9F574287628E5A3781DE94A598F6B5C229C1D2F401341A9C98936941561D6641`；fd ZIP SHA-256：`0D6DC757E55F3443AA4A152855653CC68B583CDCA745490CBBBF8E033202D0C5`。GitHub 发布结果以远端 Release 状态及附件校验为准。

## 检查依据、范围和退出条件

用户授权：「接下来把工作向发布正式版推进：完成发布前的所有检查」。路线为 `code-quality-workflow`，库收据 ST-A0 / 2026-08-18，未采用目录推荐的设计候选。目标是把可自动验证的发布要求变成可重复执行的检查，并准确保留真人和设备验收；红线是真实库、用户剪贴板、系统显示设置和外部发布。使用既有重构计划，不创建子蓝图。

本次工程决定：构建与发行元数据 Local Fix，应用数据流程 No Refactor，只增加真实磁盘失败验证和运行时辅助功能检查。限定构建配置、发布脚本、检查器、回归测试及发行文档；不改变算法、数据字段、默认界面与业务范围。总工作树预算维持 80 文件 / 18,000 行；构建版本更新是已查明的发布风险修正，不是任意升级。

原基线为 150 项单元测试、70 项生产窗口检查、两种候选打包。用户先后确认核心五步、两项自动保护；本轮进一步确认新顶栏拖动、双击最大化/还原、最小化和关闭「这些操作均正常」。确认对应旧顶栏候选 `20260922-221251-1989`，不把反馈冒充更新运行时后的整包验收。

## 已发现及处理的问题

| 项目 | 证据与处理 |
| --- | --- |
| P1：旧候选携带较早的运行时 | 旧机器 SDK 9.0.101 / Runtime 9.0.0。微软发布元数据当前 9.0.20，发布日 2026-09-08，属于安全更新。采用校验 SHA-512 的官方 SDK 9.0.318 ZIP，解压到项目工具目录，固定运行时 9.0.20；不据此声称某条 CVE 在本应用中可以利用 |
| P2：程序图标未进入 EXE | WPF 工程条件查找工程目录下不存在的 icon.ico，已改为引用根目录现有图标 |
| P2：系统支持声明不实 | 清单仍包含 Windows 8.1，已移除；只保留 Windows 10/11 支持声明 |
| 发布依赖不可追溯 | 添加 SDK 版本文件、运行时最低版本、四项目依赖锁文件；正式检查使用 locked restore 并查询间接依赖 |
| 发布验证缺少归档/重开证据 | 添加统一的本地发布检查入口，检查压缩包解压后运行、重复实例、重复演示和文件完整性 |
| P3：测试框架 xUnit v2 被标记 Legacy | NuGet 返回 xunit 2.9.2 及 4 个同版本间接包已弃用，推荐 v3。仅用于测试，不进入发行包，无已知漏洞报告；本轮保留并在流水线明确记录这个精确版本例外，后续另行迁移测试框架。其他弃用或漏洞仍会阻断 |

## 自动验证

首次补测：155 项 Release 测试通过；运行时 9.0.20 的生产窗口 79 项检查通过；Release 构建零警告/错误。新增磁盘测试覆盖真实文件占用下保存/改密失败、备份失败阻止替换、恢复副本可解密、损坏或错误密码不改写原库、CSV/设置失败保留旧文件。新增窗口检查验证密码控件的自动化接口不可读取明文，以及标题栏按钮的名称和调用接口。

完整候选包流水线通过，证据目录：`artifacts/release-check-20260922-223757-8966/`。`release-checks.json` 记录 155 项单元测试、两种产物各 79 项窗口检查，以及 58 条打包/归档/进程检查。所有这些自动检查通过，但总体状态仍为 `automated-checks-passed-manual-gates-pending`。

两种包的解压产物均完成自检、合成演示启动、第二实例拒绝、重复演示拒绝覆盖、正常锁定启动、退出及库文件逐字节不变检查。归档逐文件 SHA-256 与清单一致；不含库、设置、导出 CSV 或证书私钥。随包包含使用/升级说明及 .NET、System.Drawing.Common、SystemEvents 的许可和通知。候选 EXE 签名状态为 NotSigned。

另将该检查进程的 `DOTNET_ROOT_X64` / `DOTNET_ROOT` 指向不存在的目录并禁用多级查找，解压的 sc 包仍以 .NET 9.0.20 通过 79 项窗口检查，证据为 `sc-without-shared-runtime/checks.json`。这补强了自包含运行时证据，但没有冒充另一台干净 Windows 的兼容性验收。

| 候选 | ZIP 字节数 | ZIP SHA-256 |
| --- | --- | --- |
| `api-key-manager-2.0.0-win-x64-fd-candidate.zip` | 237,766 | `C350CD87B97022E23448EFDD6FC496C625D2779563E52A24C8320423D41ADEB9` |
| `api-key-manager-2.0.0-win-x64-sc-candidate.zip` | 54,607,634 | `892C12BC13E4C8817FEDDB7BAE912E9025EAF9A8E0E8E44BAB9785D71D4E7837` |

推荐交接自包含包。原始发布目录中 fd 为 1 文件 / 457,723 字节，sc 为 6 文件 / 64,649,650 字节；归档额外加入说明和许可，所以不要混淆原始二进制体积与 ZIP 体积。版本资源为 2.0.0，已从实际 EXE 提取并查看应用图标。

SDK 官方下载地址、SHA-512 比对和支持结束日期见 `toolchain-provenance.json`；依赖审计完整结果见 `vulnerable.json` 和 `deprecated.json`；测试结果为 `tests.trx`；构建使用警告视为错误。构建时的全部跟踪/未跟踪文件 SHA-256 见 `source-inventory.json`，用于识别未提交工作树；EXE 中的 Git 后缀只表示基础提交，不代表未提交源码已经入库。报告和续接记录会在检查后更新，不修改候选二进制。

本轮审阅重点：库加密与设置/路径、Application 保存/导入/改密/导出、生产 ViewModel、窗口关闭/锁定/顶栏、剪贴板与闲置服务、包配置、测试和发布脚本。全部测试均运行，但没有逐行审计每个历史测试和原型生成器，也没有对运行时第三方源码做独立安全审计。输出包不包含原始测试目录和合成数据；程序集仍保留既有诊断命令。

检查中曾遇到工具问题，均留下失败报告：并行构建造成 obj 文件争用；单文件全局参数改变类库依赖图造成锁文件拒绝；Windows PowerShell 5.1 对 JSON 数组包装方式导致脚本类型错误。均在顺序构建、限定项目属性和修正数组读取后通过最终完整流程；没有删除失败记录或把失败计为通过。

## 人工及设备检查

| 项目 | 当前证据/状态 |
| --- | --- |
| 核心五步与自动保护 | 用户此前明确通过；保留原始会话记录 |
| 新顶栏基本操作 | 用户本轮明确通过；不是代理系统输入模拟 |
| 100/125/150/200% 缩放 | 未完成系统缩放切换；本机窗口检查测得实际 DPI 100% |
| 多屏、混合 DPI、贴边 | 未完成对应设备实测；只检测到一块 1920×1080 屏幕，可用区域 1920×1032 |
| 全程键盘、读屏 | 有真实控件自动化接口检查，但完整 Tab/Shift+Tab、快捷键和 Narrator 朗读未实测 |
| 干净机器/Windows 10 | 两种本机产物和归档检查；没有另一台 Windows 10 或无预装 .NET 的干净虚拟机证据 |
| 极端故障 | 文件占用已实测；真实断电、磁盘满、网络共享、剪贴板长期占用未实测 |
| 签名与公开渠道 | 未提供发布者证书；候选 EXE 未签名，不假装拥有发布者身份。公开渠道、签名安排由维护者决定 |

原生桌面驱动当前不可用。不能把程序发送给自己的窗口消息、离屏截图或注入时间包装成人工键鼠、混合缩放或读屏通过。Win11 悬停最大化按钮的布局面板没有实现，作为已知限制写入随包说明。

## 发布前最后的人工检查

使用本次生成的自包含候选，在合成演示库完成：

本机入口为 `artifacts/release-check-20260922-223757-8966/acceptance.cmd`，合成库密码 `demo`；可执行文件哈希为 `EEEF6AAF54D9AF8F8C2A47DAE16D9E179CD1A8755B26AC154F3C9D31F0031619`。不要把真实密钥录入测试目录。

1. 当前电脑分别切换 100%、125%、150%、200% 系统缩放，在屏幕可用区域满足 900×600 逻辑像素时，检查顶栏、编辑保存、设置确认、锁定解锁均不裁切。分辨率不满足时记录不支持，不把该项填通过。
2. 有第二块屏幕时来回拖动并最大化/还原、贴边；检查任务栏和窗口按钮。没有第二块屏幕则明确未测。
3. 只用 Tab、Shift+Tab、Enter、Escape 和说明中的快捷键，完成解锁、搜索、新增、保存、设置、锁定；打开 Windows Narrator，确认输入框名称、密码遮蔽和窗口按钮可识别。
4. 在目标 Windows 10/11 的干净机器解压整个自包含包，运行演示并重开。该项确认运行时和原生组件已包含，不需要另装 .NET。

所有结果要对应候选 SHA-256。自动化不设置 `driver-accepted`；未取得上述证据时，结论必须是「自动化通过、待环境验收」，不能写「所有发布前检查已通过」。

## 最终范围与当前结论

完整工作树范围包含前面已授权的整套重构、已暂存/未暂存及全部未跟踪维护文件。范围检查使用完整 UTF-8 解码的 numstat 快照，避免截断中文导致二进制误报；输出为 `artifacts/release-scope.numstat` 和 `release-scope-report.txt`。当前 76 文件、约 9,650 行增删，在 80 文件 / 18,000 行预算内。`git diff --check` 通过。

可在本机完成的发行自动化检查已通过，候选包已具备可复核的文件清单、校验值和操作说明。当前还不能宣布「全部发布前检查通过」：环境验收及签名/分发安排仍需对应的人和设备；本轮不会据此自行公开发布。已知 xUnit 测试依赖弃用和 .NET 9 剩余维护期限保留在报告，不扩成临时大版本迁移。
